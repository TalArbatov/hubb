import * as TYPES from '../constants/actionTypes';

const defaultState = {
    isLoading: false
}

const commonReducer = (state = defaultState, action) => {
    switch(action.type) {
        case TYPES.
        default:
            return state;
    }
}
